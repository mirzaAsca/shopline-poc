---
description: Phase 2 — implement the single most-important unchecked task from the active specs/ file, deploy, test, validate, check off, commit + push.
---
Read and follow `prompts/PROMPT.md` exactly. Load `CLAUDE.md` + referenced docs + the active `specs/*.md` + `LEARNINGS.md`, pick the ONE most important unchecked task, search before assuming, implement it (modular/brand-neutral), deploy the changed theme/content to SHOPLINE as required, write + run its `tests/*.test.js`, run the desktop+mobile visual-diff, then tick the checkbox in that spec file, append to `LEARNINGS.md`, commit and push to Git. One task only, then stop.
