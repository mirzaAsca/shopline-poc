---
description: Phase 1 — analyze a source site and generate a descriptively-named specs/ plan file (all checkbox items). Does not implement.
argument-hint: <source-url-or-html> [1:1|redesign]
---
Source / mode: $ARGUMENTS

Read and follow `prompts/PLAN.md` exactly. Analyze the source above and produce a new, descriptively-named spec file in `specs/` (e.g. `specs/01-full-initial-migration.md`) per `docs/spec-template.md`, with every item as a checkbox. Plan only — write the spec file and stop; do not implement theme code.
