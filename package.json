{
  "name": "shopline-migration-tooling",
  "private": true,
  "type": "module",
  "description": "Tooling for to-SHOPLINE Bottle migrations: visual-diff QA harness + Playwright tests. The theme itself is not an npm package; this only powers scripts/ and tests/.",
  "scripts": {
    "test": "playwright test",
    "vqa:capture": "node scripts/visual-diff/capture.mjs",
    "vqa:compare": "node scripts/visual-diff/compare.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "playwright": "^1.48.0",
    "pixelmatch": "^6.0.0",
    "pngjs": "^7.0.0"
  }
}
