# Runbook: Migrate a page
> STATUS: STUB
**Merge sources:** old docs/07 pipeline (phases 0–7) + Playbook §11 (build phases A–G) + migration-decisions.
Flow: intake → scan* → extract* → map → build (modular sections) → deploy → validate (desktop+mobile) → auto-publish → manual live QA. (*source side, separate workstream.)
